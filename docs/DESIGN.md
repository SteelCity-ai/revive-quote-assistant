# Base app design

Visual reference: `C:\Users\mike\.codex\generated_images\01a0729f-9972-7522-9b69-dcedc997f158\exec-827c22a5-113a-426f-a2a7-655eb71ad60b.png`.

The generated concept establishes a white mobile interface, dark navy type, teal selection/action color, three full-width job rows, genuine empty quote state, clear settings entry and a bottom Start quote button. All UI text and controls are native React/HTML. Lucide icons share a consistent outline style. No invented customer records or metrics are seeded.

Intentional implementation adaptations: use a simple home symbol with the text wordmark instead of the image-generated brand mark; use the existing official Revive logo on the customer document. On desktop, move the saved quotes into a second column and show a contextual quote summary beside the guide. Put settings under the job choices so the available functions are easy to discover. Subsequent guide, pricing, review and settings screens extend the same tokens to support required workflows.

Palette: navy #143853, teal #087f78, gray borders #dce3e8, white controls and a pale gray desktop canvas. Manrope headings with DM Sans body. Touch targets are generally 44px or larger. Mobile inputs use 16px text to avoid iOS focus zoom. All uncertain values and unconnected services are explicit.
